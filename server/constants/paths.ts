import * as os from "os";

export const OS_HOME = os.homedir();
export const ROOT_FOLDER = `${os.homedir()}/Documents/acfun-electron`;
export const PREFERENCE_CONF = `${ROOT_FOLDER}/.preference.conf`;
